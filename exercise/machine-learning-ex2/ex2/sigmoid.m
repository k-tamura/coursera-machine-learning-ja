function g = sigmoid(z)
%SIGMOID シグモイド関数を計算する
%   g = SIGMOID(z) はzのシグモイドを計算します。

% 次の変数を正しく返す必要があります
g = zeros(size(z));

% ====================== ここにコードを実装する ======================
% 指示: zの各値のシグモイドを計算します（zは行列、ベクトルまたはスカラーが
%       指定可能です）。


ZS99mNVpvYOaj7V8puB/BYcn86bWI1I=


% =============================================================

end
