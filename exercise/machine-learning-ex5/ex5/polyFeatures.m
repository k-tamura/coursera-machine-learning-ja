function [X_poly] = polyFeatures(X, p)
%POLYFEATURES X（1次元ベクトル）をp乗にマッピングする
%   [X_poly] = POLYFEATURES(X, p)は、データ行列X（サイズm×1）を取り、
%   各サンプルを次の多項式のフィーチャーにマッピングします。
%   X_poly(i, :) = [X(i) X(i).^2 X(i).^3 ...  X(i).^p];　　　
%


% 次の変数を正しく返す必要があります。
X_poly = zeros(numel(X), p);

% ====================== ここにコードを実装する ======================
% 指示: 与えられたベクトルXに対して、行列X_polyを返します。
%       ここで、xのp番目の列には、pの次数に対するXの値が入ります。
%
% 


ZGAymI1proyLnfRWreA6Xa9QrrOTc0GLM47WGPjk8czxVkkRgKMASg==



% =========================================================================

end
